{{> partials/frontoffice/header }}

    <main>
        <section class="page-title-and-back-btn">
            <button class="back-btn">
                <img src="/public/images/icon-back.svg" alt="Botão Voltar">
            </button>
            <h1>Perfil</h1>
        </section>

        <section class="profile-container">
            <div class="profile-info">
                <h2 class="profile-user-name">{{user.nome}}</h2>
                <div>Username: 
                    <span class="profile-user-username">{{user.username}}</span>
                </div>
                <div>Email: 
                    <span class="profile-user-email">{{user.email}}</span>
                </div>
            </div>
            <div class="profile-options">
                <button class="profile-btn" onclick="window.location.href='/logout'">
                    Sign Out
                    <img src="/public/images/icon-sign-out.svg" alt="Sair">
                </button>
            </div>
        </section>
        

{{> partials/frontoffice/footer }}